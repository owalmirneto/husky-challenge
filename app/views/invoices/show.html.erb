<div class="mt-3">
  <%= link_to_back(invoices_path) %>
</div>

<h1><%= t('.title') %></h1>

<%= render(InvoiceDetailComponent.new(invoice: @invoice)) %>

<%= link_to_destroy(@invoice) %>

<%= @invoice.link_to_file_url %>
